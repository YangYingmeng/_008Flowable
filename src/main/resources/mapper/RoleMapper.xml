<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yym.flowable.mapper.RoleMapper">

    <select id="getAllRoles" resultType="com.yym.flowable.model.Role">
        select * from role;
    </select>
</mapper>
